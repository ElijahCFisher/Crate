{
  "name": "crate-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server for the Crate food-rating app — search, add, and update ratings via the existing crate-server Worker/Drive backend.",
  "scripts": {
    "login": "node src/login.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.24.0"
  }
}
